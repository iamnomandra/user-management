{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "user-management",
            "module": "uvicorn",
            "type": "debugpy",
            "request": "launch",
            "args": ["main:app", "--reload", "--port", "8000"],
            "envFile": "${workspaceFolder}/.env", 
            "jinja": true
        }
    ]
}
